import { ConfigType } from "@markdoc/markdoc";
import DashedList from "../schema/DashedList.markdoc";
import { spacer } from "../schema/spacer.markdoc";

export const config = {
  tags: {
    DashedList,
    spacer,
  },
} as ConfigType;
